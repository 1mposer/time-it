node_modules/

# Environment / secrets
.env*
!.env.example
.flaskenv*
# Owner-private notes — never commit (bundle-ID/APNs facts, session-to-session)
docs/OWNER_NOTES.local.md

# OS files
.DS_Store
Thumbs.db

# Xcode / Swift
*.xcuserstate
xcuserdata/
DerivedData/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
.build/
.swiftpm/
# Xcode DerivedData from `xcodebuild -derivedDataPath build`
ios/TimeIt/build/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Generated review reports
docs/review/out/

# WIP alternate weather-provider (IQAir) experiment — not ready to merge
cli-iqair.js
src/weather/adapters/iqair.js

# Codebase Teacher — local learning tool (learn.html + regenerable source), not committed
learn/

# Claude Code local tooling — not committed
.claude/settings.local.json
.claude/projects/
# Vendored apple-hig HIG skill (unlicensed Apple-derived reference; keep local per agent)
.claude/skills/apple-hig/
