# STATUS log — chronological edit history

> ⚠️ **CONTAMINATION WARNING — reading this file can be a source of context contamination.** This is the entire log of all updates: it records what *was* true, superseded entry by entry, so almost everything below is **stale by design**. **If you do not have explicit permission from the user to read this file, you must discard all context read from within it (STATUS_LOG.md) — this file holds stale information.** Current truth lives in [`STATUS.md`](STATUS.md). If the user has requested that you update this file with status, you are to place the entry in the most chronological place within this file (newest entries at the bottom, ordered by the date the entry is written) — do not choose a random spot.

> Append-only journal of changes to [`STATUS.md`](STATUS.md) and the surrounding design docs. This is **provenance, not orientation** — to find out *where the project is now*, read [`STATUS.md`](STATUS.md), not this file. Newest entries at the bottom.

- **2026-06-18** — Created. Drift table + locked-decisions index + audit blockers (B2, provider-verify, stateless wording, metric-flip, catalog refresh). Paired with: CONTEXT.md/CLAUDE.md routing banners, deletion of three empty `docs/agents/*.md` orphans, two in-place grill corrections (line 37, Q3.i).
- **2026-06-19** — B2 resolved. Added [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md) pinning the day-bucketed `/rating` wire shape (flat 7-entry `days[]`, day-0 card, dense null days, no top-level pointer). Flipped §4 B2 blocker→resolved, §5 iOS #5a blocked→unblocked, drift-table `/rating` row, locked-decisions index. Paired with grill line 37 update.
- **2026-06-19** — Provider verification done (Meteosource `flexi` ~164 clean hourly). Reframed ADR-0003 + ADR-0004: **168 is a ceiling, not a count** — horizon provider-determined, last day partial, decoder/snapshot must not hardcode 168/7, no fabricated hours. Added adapter-boundary principle to CONTEXT.md (Adapter term). Updated drift table (Forecast/Index rows), locked-decisions (+horizon + adapter-boundary), §4 provider flag DONE, §5 timeline unblocked.
- **2026-06-19** — Day-bucketing rule settled: **location-local calendar days**, not `floor(index/24)` blocks. Timezone = provider's IANA `timezone` field (Meteosource `timezone=auto`), normalised at the adapter boundary; a new **time-boundary module** tags each hour's `localDay`; engine groups by it. `dayIndex` = 0-based local-calendar-day ordinal (0..6 or 0..7; today + tail partial), `days.length` NOT a closed form over `hours.length`; `startIndex`/`endIndex` stay global; `localDay` internal (not on wire); response gains top-level `timezone`; client renders in that zone. Amended ADR-0003 (rule + worked 8-bucket example + fetch wrinkle + UTC-instant landmine), ADR-0004 (killed false `days.length` formula, +`timezone`, +three pins), CONTEXT (sibling time-boundary principle + Forecast-start rendering note), this drift table + locked-decisions. **Open:** partial-day-0 card rule (parked); custom-activity request schema (Phase-2 gate, unchanged).
- **2026-06-19** — Partial-day-0 card rule settled (was parked). Card = `days[0]` with a **client-side soonest-actionable fallback**: today if it has a window, else the earliest non-null day (labeled by name), else "No window in the next 7 days." Soonest, *not* best (does not reopen `bestDayIndex`); pure client display rule, **no wire change**; Daily Digest stays strictly day-0. Amended ADR-0004 sub-decision 4 + locked-decisions here. **Open now:** custom-activity request schema (Phase-2 gate).
- **2026-06-19** — Session close-out: added the **▶ NEXT** pointer to §5 (request-schema grill → ADR-0005, then Phase 1 → Phase 2 → Job B; implement coupled core as one stream). Boundary/horizon design fully settled & committed; next work starts in a clean session on `main`.
- **2026-06-20** — Custom-activity request schema grilled & pinned → **[ADR-0005](adr/0005-custom-activity-request-schema.md)** (the input-side twin of B2; Phase-2 gate cleared). Decisions: POST body `{ lat, lon, activities[] }` (lat/lon in body, no version/timezone field — tz resolved server-side from lat/lon); per-activity `{ id→activityId echo, label, displayMetrics (parent superset, owns order + show-but-don't-judge), thresholds (map, evaluated subset, invariant `keys ⊆ displayMetrics`), window? }`; threshold value shape verbatim from engine (`required` mandatory, `requireTrue` rejected, bound-less numeric rejected); **window = half-open `[startHour,endHour)` integer local hours**, client sends raw wall-clock, server converts (time-boundary module tags `+localHour`) — absent = whole day, `start<end` = same-day, `start>end` = **midnight-wrap/nocturnal**, `start==end` = 400; validation **atomic** (all-or-nothing) + **structured-array** error body uniform across 400/502/500; **coming-soon/unknown metric = hard 400** (the false-Perfect guard, defence-in-depth behind unclickable authoring); abuse ceiling ~50; tier/quantity NOT server-enforced (client-side per grill Q3). Two amendments triggered: **ADR-0003** (wrap-gated night-stitch — one controlled cross-midnight exception, stitch the *night* not eligible-hours, `dayIndex` = evening's day) and **ADR-0004** (`days.length` is **per-activity**; nocturnal buckets by night so no cross-activity equality; `{7,8}` assertion widened). Updated header "Last updated" line, drift table (`/rating` row +ADR-0005 in/out), locked-decisions (+request schema, +night-stitch), §5 NEXT (grill done → begin Phase 1), and sharpened the §4 stored-activity metric-flip blocker (atomic whole-request-rejection blast radius). CONTEXT.md/CLAUDE.md glossary migration still deferred (Job B, post-code). *(2026-06-20 self-audit pass: fixed stale "7-entry `days[]`" in §4/§5 → variable-length; compressed the over-restated request-schema bullet to a pointer.)*
- **2026-06-20 (audit + scope)** — Ran a 3-agent audit of the ADR set + STATUS for cross-contamination and #5a-readiness gaps. **No internal BLOCKERs in the ADR design set.** Applied hygiene fixes: ADR-0004's request-body cross-ref repointed ADR-0002 → **ADR-0005**; both `{7,8}` claims back-referenced to the per-activity amendment; pinned the **nocturnal tail-hour rendering rule + "Tonight" label**; precise Issue #8 link; same-day caveat on the `duration` identity. Pinned **per-hour `hours[]` shape** (ADR-0004 amendment 0620b): field set carries forward, AQ/marine additive/tolerant decode, **UTC `hour` field dropped** (footgun under location-tz rendering — reversible if a non-display use surfaces). **Scope decided:** iOS **#5a ships core read-only first**; authoring + POST encode + metric catalog (**ADR-0006, pending**) + error-`code` enum + seed Template values defer to the **#5b/Phase-2 wave** — all logged in §4 as non-#5a-core flags. #5a-core is stable ground; next is **Phase 1** (or begin the #5a-core decoder against the ADR-0004 shape in parallel).
- **2026-06-21 (doc hygiene)** — Extracted this edit log out of STATUS.md into this file (STATUS_LOG.md) to restore STATUS's "orientation only, no chronology" charter; STATUS keeps the rolling **Last updated** banner + a `## History` pointer here. In §4, trimmed the two no-longer-open items to pointers (**B2 RESOLVED** → ADR-0004; **provider-verification DONE** narrative → here, keeping only the open Air Quality + Marine tail) and **deleted** the redundant "Grill self-defects fixed in-place" entry (already captured in the 2026-06-18 entry above). §3 locked-decisions / banner restatement trim deferred by decision (cold-pickup tradeoff). Done on `cleanup`/`main` before cutting the rebuild branch.
- **2026-07-03 (#5a spec reconciliation + entry-point refresh)** — Rewrote the stale **`implement-spec-issue-5a-ios-core.md`** and the shared **`design-decisions-issue-5.md`** to the shipped Phase 1/2 contract (`2a5a50b`), so #5a has a stale-less entry point to build the SwiftUI app. Reconciled old→new: `GET ?lat&lon` → `POST /api/v1/rating` body `{ lat, lon, activities[] }`; server-side 5-activity list → **client-authored seed Templates** (Cycling + Fishing-lite, **live metrics only** — verified against `metricCatalog.js`; provisional thresholds flagged as a #5b pin); single top-level `rating`/`startIndex` → per-activity **`days[]`** (variable length; never hardcode 7) + top-level **`timezone`** (render clock/day labels in the location zone; `hour` field dropped); removed all accounts/auth (AuthManager/AppState/Keychain/SignInView), the bottom tab bar, and `isPro`/`-pro`/PRO-badge; Settings → top-right gear; card = soonest-actionable day, detail = 7-day timeline. Cross-checked every field name against CLAUDE.md's response contract. Then refreshed the **STATUS.md** entry point: banner + §5 ▶ NEXT flipped from "pending `pre-merge-main → main` merge" (that merge already landed, `9a9c60b`) to "build #5a from the reconciled spec"; #5a §5 bullet marked spec-reconciled. Full suite still green (**110**). **Still stale (out of scope, own pass):** `implement-spec-issue-5b-ios-personalization.md` (authoring/Pro/metric-picker) — untouched here.
- **2026-07-03 (grill ↔ STATUS de-duplication)** — De-conflicted `personalization_grill.md` against STATUS: the grill was duplicating STATUS §4/§5's *live open-work tracking* and had drifted stale (three "pending" items were already done post Phase 1/2). Restored the intended split — **STATUS = current state, grill = frozen rationale**. Changes: (a) moved the two grill-only open items (**v1-vs-fast-follow launch sequencing**; **Template-override + client sync schemas**) into STATUS §4 so nothing is lost, then (b) in the grill, reframed the cold-start "PENDING" list + "Session status" next-actions as an annotated grill-close *snapshot* that points to STATUS for live status (✅ request schema pinned/ADR-0005, ✅ CONTEXT.md migration done, ✅ Meteosource verification done); retitled "CONTEXT.md migration pending" → "migration — DONE (Phase 1/2)"; added the missing **ADR-0004/0005** rows to the canonical artifact table and fixed its stale "CONTEXT.md not yet updated" note; added one "superseded by ADR-0005" pointer on the stale `window:{startIndex,endIndex}` data-shape line. Q1–Q9 rationale and the "Decisions locked" table left untouched (the latter's overlap with STATUS §3 is the intended point→detail split, not drift). Verified CONTEXT.md is in fact migrated before writing the "DONE" claims.
- **2026-07-06 (#5a core iOS app built)** — Built the **#5a core SwiftUI app** at `ios/TimeIt/` from the reconciled spec, TDD (all tests written first, red run, then implementation to green): **62 tests green** — 55 unit (decode contract incl. 7-vs-8-day `days[]`, global-index guard, nullable-trio null→nil, chip tier boundaries, `formatted(for:)` em-dash, `TimeDeriver` vs the ADR-0003 worked example + device-zone independence, seed-Template ADR-0005 tripwire, ViewModel load/Dubai-fallback/soonest-actionable/window-slice) + 7 XCUI acceptance (no gate/tab bar, header placeholders + gear, cards in request order/no PRO, day labels + timeline + chips, detail push/pop, Settings sheet, error state). Zero build warnings. Environment: installed the iOS 26.5 simulator runtime (Xcode 26.6 ships without one); no xcodegen on the machine, so the Xcode project is hand-written in the synchronized-folder format (objectVersion 77) with a **shared scheme committed** — deliberate deviation from the spec's `.gitignore` list: `*.xcscheme` not ignored (`xcodebuild test` requires the shared scheme; user schemes are covered by `xcuserdata/`). The XCUI suite is **hermetic** — DEBUG-only `MockRatingService` + `StaticLocationProvider` behind `UITEST_MOCK_SUCCESS`/`UITEST_MOCK_FAILURE` launch args — because the repo has no `.env`/Meteosource key; **live end-to-end against `localhost:3000` is unverified** (flagged in the STATUS banner + §5 ▶ NEXT). TDD caught one real defect: the header ignored the top safe area per the web-mockup-derived measurements, leaving the gear under the status bar (untappable — the XCUI tap failed); fixed by keeping header content in the safe area with only the gradient extending behind the status bar. Two accessibility-tree test fixes (a container `accessibilityLabel` overriding child static texts; `LabeledContent` combining away the "Version" row). STATUS banner + §5 flipped to "built, pending audit + live verification"; #5b spec still stale (unchanged, its own pass).
- **2026-07-12 (#5b spec reconciliation + scope lock)** — Audited the shipped #5a app against the spec (2 agents: requirements-from-docs + code-inventory) after the owner flagged "missing add-activity button." Finding: **#5a is faithful to spec — authoring is #5b, deliberately deferred**, but the on-disk `implement-spec-issue-5b-ios-personalization.md` was **pre-rebuild and wrong** (sign-in/`AuthManager`/onboarding over a fixed 5-activity *server* list + per-activity `isPro` lock overlays on `boat-fishing-pro` — all cut by ADR-0001/0002/0005 + design-decisions Q3 — and it never contained the authoring the owner actually wanted; design-decisions:6 literally tags #5b "authoring/Pro, **to be written**"). **Rewrote it from scratch** to the shipped architecture: client-side **activity authoring** (add/edit/delete from Template or scratch; `AuthoredActivity` model → projects to `ActivityInput`; metric picker; threshold + optional-window editor incl. nocturnal wrap), **local persistence** (`ActivityStore`/UserDefaults, first-launch-seeded with the two #5a Templates), `PreferencesStore` home-location picker, and a client-side ADR-0005 validation mirror (the atomic-400 guard — an in-app-authored activity can never 400 the whole dashboard). **Three scoping calls grilled + locked with the owner:** (1) metric picker = **client-side static catalog behind a `MetricCatalogProviding` swap seam** (owner asked explicitly for a clean upgrade path to the unbuilt `GET /api/v1/metrics`); (2) **Pro/StoreKit DEFERRED** — no LIVE metric is Pro-only (premium metrics are coming-soon/400'd), only quantity is gateable → soft constant cap now, §8 retains the full StoreKit spec for a post-#7 wave; (3) **local persistence only**, no cloud sync (that model stays undesigned, §4). An **advisor review pass** caught + folded in two #5a-carryover gaps verified against the code: all 9 LIVE metrics already render (`HourlyWeather.formatted(for:)` + `MetricTier` neutral fallback — so the picker isn't restricted and rendering isn't re-derived), and `TimeDeriver.dayName` has **no nocturnal "Tonight" labeling** (never exercised — both #5a seeds diurnal) → added as an explicit #5b work item + test. Then updated **STATUS.md**: banner (new #5b-ready headline, #5a pushed to Prior), §4 flags (metrics-route no longer blocks the picker via the seam; error-`code` enum downgraded to nice-to-have; cloud sync explicitly deferred by #5b), §5 ▶ NEXT (#5b now build-ready — hand to Fable once #5a is audited + verified live). #5a build itself unchanged (still pending audit + live verification). **ROADMAP #5b line refreshed** off the stale sign-in framing.
- **2026-07-12 pm (#5a live-verified — uV-null bug found + fixed, live header weather added)** — Ran the #5a app end-to-end against a live Meteosource-backed `localhost:3000` (owner supplied the API key). **Found a release-blocking bug the 62 hermetic tests missed:** Meteosource returns `uv_index: null` at night; `meteosource.js` passed it through un-guarded, but `uV` was typed non-nullable in both the contract and the iOS `HourlyWeather` model, so a single null `uV` threw in `JSONDecoder.decode(ForecastResponse.self)` → the dashboard hit its error state on *every* real request (every 7-day forecast spans night; the null scan showed 59/164 hours null). Root-caused via the error copy (`invalidResponse`, not the ViewModel's generic "unable to reach" → the request succeeded and the decode failed) + a per-field null scan. **Fixed in two layers (owner chose both):** (1) **backend** `uV: (h) => h.uv_index ?? 0` — nighttime UV genuinely is 0, keeps `uV` non-null on the wire, contract stays truthful (+4 adapter tests, backend now **114**); (2) **client hardening** — all 7 metric fields → `Double?` with a tolerant custom `init(from:)` (`decodeIfPresent` every metric; `moon`→[], flags→false, placeholders→0; only `index` required), so any unexpected null renders "—" instead of blanking the app (+2 iOS tests). **Also implemented live header weather** (owner ask, previously deferred): `HeaderView(currentHour:)` now renders the location's current-hour temp/wind/humidity from `hours[0]` with `—` fallback; updated the XCUI header test to assert the mock fixture's real values (iOS **64** tests, all green). Re-verified live: real cards + real-hour-span timelines + populated header (Dubai 33°C/4 km/h/54%, matching an independent curl of `hours[0]`). Docs updated: **STATUS** banner + §5 (▶ NEXT flipped to "build #5b" — live-verify gate CLOSED); **CLAUDE.md** (uV `?? 0` adapter behavior + the client's null-tolerant decoder note + field-type line); **implement-spec-issue-5a** (header no longer deferred; `HourlyWeather` all-metrics-optional note; AC); **design-decisions-issue-5** (header now live). Backend server left running (bg) for further poking. Uncommitted → committed on a branch this session.
- **2026-07-12 (pm, #5a audit + real-response regression net → merged to `main`)** — Independent audit of the live-verify commit (`08dc038`) before it merged to `main`: re-ran both suites from scratch (backend **114**, iOS **64**, no trust in the commit message), read every changed file against CONTEXT.md/ADR-0003/0004/0005 — all four claims (adapter `uv_index ?? 0`, all-7-metrics-`Double?` tolerant decoder, live `HeaderView(currentHour:)`, doc updates) verified honest. Caught **3 stale STATUS.md lines** the #5b reconcile left contradicting the live-verified headline (uncommitted→merged; "live path unverified"→verified manually; #5a "pending audit"→audited) and fixed them; **gitignored** a WIP `iqair` alternate-provider experiment (`cli-iqair.js` + `src/weather/adapters/iqair.js`) so it can't reach `main`. Merged `08dc038` + the cleanup (`9ec4cb6`) fast-forward to `main` and pushed. Then closed the one audit gap flagged earlier (hermetic mocks were daytime-only → missed the night uV-null): captured a **real 166-hour response** from the live backend (Dubai, 118 night hours, 8-day buckets), committed it **verbatim** as `RealBackendResponseFixture.swift`, and added 3 tests (`RealBackendResponseTests`) — full real-scale decode, night-spanning decode, and null-injection survival — so the decoder is pinned to the actual provider wire shape, not a hand-written mock. iOS **67** tests green; `/code-review` (high) clean; committed (`a9a1bdf`) + pushed. #5a is now audited, hardened, and on `main`; **#5b is the direct next build.**
- **2026-07-13 (#5b built — activity authoring & personalization)** — Built **#5b** TDD against the reconciled spec (tests written first across 6 new + 4 extended suites, then implementation to green): **iOS 133 tests** (119 unit + 14 XCUI), zero warnings, backend untouched (114). Shipped: **`AuthoredActivity`** (Codable editable superset; `activityInput` projection drops `iconSymbol`/`templateOrigin`, includes `window` only when present; `copyFromTemplate()` = fresh UUID + `templateOrigin`) with a **client-side ADR-0005 validation mirror** (`validationIssues(against:)` — the atomic-400 guard; flag shape can never emit `requireTrue`); **`ActivityStore`** (`@MainActor` singleton, UserDefaults key `authoredActivities`, first-launch-seeded with the two #5a Templates, persisted-empty ≠ first-launch so an emptied list doesn't re-seed, corrupt-decode → seed fallback, `softCap = 10` under the ~50 ADR-0005 ceiling — Pro/StoreKit deferred per scoping call #2); **`MetricDescriptor`/`MetricCatalogProviding`/`StaticMetricCatalog`** (9 LIVE keys pinned by a drift-tripwire test vs `metricCatalog.js`; `moon` displayOnly, `dustAlert` flag; a test-double conformer proves the swap seam ahead of ADR-0006's `RemoteMetricCatalog`); **`PreferencesStore`** (`homeLocation: SavedLocation?` persisted; coords resolve home → GPS → Dubai) + Settings home-location search behind a **`GeocodingProviding`** seam (`CLGeocoderService` real, `MockGeocoderService` in XCUI); **authoring UI** (ghost add-card after the list — disabled with a friendly message at the cap; `AddActivityView` Template-copy/from-scratch; `ActivityEditorView` Form with metric picker, per-metric threshold rows as text-bound drafts, required toggles, window toggle + hour pickers with same-day/overnight/invalid hint, inline "why Save is disabled" issues, destructive delete + confirmation); **empty-list state skips the POST** (`DashboardViewModel.hasActivities`; deleting the last Activity renders the empty state, never an empty `activities[]`); **store/home-location mutations auto-refetch** (Combine, dropFirst); **nocturnal day labels** (`TimeDeriver.dayName(forDayIndex:nocturnal:)` → Tonight/Tomorrow night/Weekday night, threaded from the authored wrapped window into card + detail); **`ActivityIconView`** single icon seam (runtime `questionmark.circle` fallback; card prefers explicit `iconSymbol`, legacy `contains` heuristic retained as fallback); **Template catalog** grew `running` (diurnal; `figure.run` ⚠︎ pending owner SF-Symbols pass) + `stargazing` (nocturnal `22→4`, exercises the night-stitch e2e; `moon` display-only) — all four pass the extended ADR-0005 tripwire, thresholds still provisional; **`ActivityInput`/`Threshold`** now Codable with optional `WindowSpec` (key omitted when nil — pinned by encoding tests); **400 backstop** — `APIClient` parses the structured `{ errors }` envelope on a 400 into `APIError.validationRejected`, naming the offending activity via the `path` index. XCUI: `UITEST_RESET` launch arg wipes persisted state (existing #5a tests updated to include it); persistence tests relaunch **without** it; two XCUITest wrinkles fixed during the run (lazy Form rows need scroll-to before `editor.delete` exists; `confirmationDialog` buttons surface twice → `firstMatch`). Docs: STATUS banner + §4 provisional-thresholds note + §5 (▶ NEXT → #6, new #5b bullet, stale "then #5b" tail replaced), CLAUDE.md build order, ROADMAP (#5a line de-staled, #5b ✅), design-decisions manifest (Table A + running/stargazing rows, Table C + `plus.circle`/`mappin.and.ellipse`/`checkmark`, card-gear row un-stubbed, icon-seam note). Known: night-stitch rendering exercised at unit level (the XCUI mock fixture stays diurnal). **Same-session `/code-review` (high, 8-angle) found + fixed 4 confirmed correctness bugs pre-commit:** (1) non-finite threshold bounds (`inf`/`nan` parse via `Double(_:)`) passed validation but make `JSONEncoder` throw — silently killing persistence AND every POST — now rejected at both draft-parse and model validation; (2) `ThresholdDraft.format`'s `Int(Double)` trapped on huge saved bounds (`1e30`) → gear tap crashed the editor forever — now magnitude-guarded; (3) the card timeline positioned night-stitched windows against a calendar-day axis, drawing the highlight off-track (or invisible for a morning-tail window) — axis now widens to cover the window; (4) un-serialized refetches let a stale pre-mutation response overwrite a newer one — generation-guarded. Plus: GPS fix re-warmed on every load (clearing home no longer silently falls to Dubai), `DashboardView` now always observes the view model's store (two independently-defaulted stores could diverge), flag-shaped thresholds on display-only metrics rejected (false-Perfect gap), the metric→icon table de-duplicated into the catalog (chipIcon delegates), editor icon choices derived from the Template catalog (+ always include the draft's own icon), one parse+validate pass per editor render, `AuthoredActivity.blank()` shared factory, STATUS icon-seam wording narrowed to activity/metric glyphs. Two findings deliberately **not** fixed (reported): the 400-rejection error state has no edit/delete escape hatch (client stale-activity reconciliation is explicitly out of #5b scope and unreachable for in-app-authored activities); `ActivityStore.add` silently no-ops at the soft cap (the only entry point is disabled at cap; revisit with the Pro wave). Final: **iOS 133** (3 new validation regression tests), full suite re-run green.
- **2026-07-15 (#5b fresh-session review + fix batch, branch `issue-5b-authoring`)** — After committing #5b to `issue-5b-authoring` (`daabeea`), an ultra (cloud) review was attempted but failed infra-side (agents terminated, no findings; the free-quota credit was consumed — flagged to support). The owner instead ran an **independent fresh-session review** (same model, zero build context, judged from code + diff alone) — findings preserved verbatim in [`docs/review/issue-5b-authoring-code-review.md`](review/issue-5b-authoring-code-review.md). It re-confirmed one deliberately-skipped finding with a concrete failure path and surfaced **two real bugs both same-session reviews missed**, validating the fresh-eyes approach. All 7 findings triaged + fixed: (1) **`ActivityStore.add` → `@discardableResult Bool`**; `AddActivityView` only dismisses on success and shows an "Activity limit reached" alert otherwise (the cap can race in via a second scene between opening the sheet and saving — previously the activity was silently dropped and the sheet closed as if saved). (2) **Validation mirror enforces threshold-shape ↔ metric-kind** (`AuthoredActivity.validationIssues`): a flag-shaped threshold on a numeric metric — which `checkThreshold` server-side can *never* fail (silent false-Perfect) — and numeric bounds on a flag metric are both rejected. **The equivalent server-side gap in `validateRatingRequest` is real and recorded as Issue #8 scope** (needs kind awareness in `metricCatalog.js`); ROADMAP #8 line + STATUS §5 handoff note (g) updated. (3) **Late-GPS-fix reload**: `LocationProviding` grew `locationPublisher`; `DashboardViewModel` re-rates when a fix lands that (a) isn't overridden by a home location and (b) moves >~1 km from `lastFetchedCoordinate` — the distance gate (not `dropFirst`) is what prevents a request→fix→request loop, since every load calls `requestLocation()`. Fixes the previous fix's overpromise: clearing home (or first-ever fix) now genuinely re-rates at the real location instead of sticking on Dubai until a manual action. (4) **Metric name table de-duplicated**: `HourlyWeather.label(for:)` deleted (it had already drifted from the catalog: "Wind"/"Rain"/"Moon"/"Dust" vs "Wind Speed"/"Rainfall"/"Moon Phase"/"Dust Alert"); `MetricCatalogProviding` grew `displayName(for:)` + `iconSymbol(for:)` and card/detail chips read those. (5) **Seam honored at chip call sites**: `ActivityCardView.chipIcon`'s direct `StaticMetricCatalog()` removed; card + detail take an injected `catalog: MetricCatalogProviding` (defaulted, same convention as the editor). (6) **Icon picker reads a dedicated manifest list** (`ActivityIconView.activityIconManifest`, mirroring design-decisions §A Table A) instead of deriving from `SeedTemplates.all` — an icon no Template uses is now pickable; new Template⊆manifest drift-tripwire test. (7) **Dead `ActivityStore.move` deleted** (zero UI call sites; restorable from git if reorder ships). Also verified sound: the review's own refutations (the `endIndex` half-open axis display; uniform-400-envelope parsing). Tests: −1 (move) +6 (2 kind-mismatch, 3 GPS-fix incl. the loop guard and home-precedence, 1 icon tripwire), cap test now asserts the Bool contract, label test repointed at the catalog → **iOS 138 (124 unit + 14 XCUI) green; backend untouched (114)**. Docs: STATUS banner + §5 ▶ NEXT (re-review sentence + handoff (g)) + #5b bullet, CLAUDE.md count, ROADMAP #8 scope line.
- **2026-07-16** — **#6 grilled end-to-end → [ADR-0006](adr/0006-device-keyed-push-evaluation.md) + four recreated specs; stale #6 spec + GitHub #5 mirror deleted.** Grill (13 decisions, owner-confirmed): server-eval device-keyed push (vs client-only BGAppRefresh / silent-push wake); full-snapshot upsert `PUT /api/v1/devices/:deviceId` (opt-in, requires real location, DELETE on toggle-off); split #6c (Postgres + registration + `apns2` seam + per-device local-6am digest) / #6d (detector: Perfect-only, once per (device, activity, bucket), buckets 0–1 — bucket-date dedup replaces the broken `startIndex` key); digest = today/tonight + week-ahead Perfect highlights (supersedes the old grill's "Digest stays day-0"); in-process node-cron, always-on, single replica; Postgres provisioned at #6c not #6b; iOS base URL via build-config switch. **Worldwide posture:** silent Dubai fallback DELETED → new #5c spec (grayed empty state, "Enable location" / "Place your own location" via MapKit city search, persisted last-resolved-location cache); Active-location chain canonized. Files: +`adr/0006`, +`issues/current/implement-spec-issue-{6b-railway-deploy,5c-location-onboarding,6c-registration-and-digest,6d-perfect-window-detector}.md`, −`implement-spec-issue-6-deploy-and-notifications.md` (superseded). CONTEXT.md: worldwide framing; +**Active location / Device / Device snapshot / Digest / Perfect-window alert**; **User preferences** reworded client-side; +push relationship line. STATUS.md: banner, §1 truth row, §3 (+2 bullets, 2 superseded notes), §4 stateless-wording flag resolved, metrics-route ADR de-numbered (0006 now = push ADR), §5 NEXT → #6b → #5c → #6c → #6d. ROADMAP: new critical path. CLAUDE.md: build order + metrics-ADR ref. Also this session: GitHub issue #5 deleted (stale pre-rebuild mirror; both 5a/5b built) + its `([GitHub](…/issues/5))` links stripped from ROADMAP/#6-spec/#3-spec/#4-spec (same treatment as #6, `f1997de`).
- **2026-07-16 (later)** — **Specs-vs-code seam audit (2 read-only subagents: ios/TimeIt/ + src/) → 11 patches applied to the four new specs before first commit.** Confirmed greenfield assumptions held (zero stale push/Keychain/db code; getWeather/evaluateAll seams as documented; cached hours read-only-safe; 100kb body limit ample). Leaks found + patched: #5c — `GeocodingProviding` is one-shot so `MKLocalSearchCompleter` can't conform (→ debounced `MKLocalSearch` conformer through the existing seam), VM has no state enum + no-location ≠ no-activities (state shape prescribed), Dubai-pinned tests must be *replaced* + new `UITEST_LOCATION` launch arg (XCUI mock location is `nil`), `LocationProviding` lacks `authorizationStatus` (seam extension added as work item), header location label is net-new, Settings search XCUI re-record; #6c — error envelope + 502/500 mapping inline in `rating.js` (shared-helper extraction step added), `validateActivities` needs `pathPrefix` param + owns `seenIds`, `window` is input-only (nocturnal copy reads the snapshot, not results), `timeBoundary.localHour/localDay(Date.now(), tz)` already cover local-6am selection (no new helper); #6d — `endIndex` EXCLUSIVE pinned (ended = `forecastStart + endIndex h ≤ now`; end clock label from exclusive bound); #6b — APIConfig `#if DEBUG` switch already built (§4 shrunk to URL fill-in; shared Info.plist kept as primary). No ADR changes — architecture survived contact with the code.
- **2026-07-16 (pm)** — **Second independent audit (fresh session, adversarial re-verification of ADR-0006 + the four specs against real source; pre-verified "audited 2026-07-16" claims re-checked anyway — all held) → 8 further findings, all patched this session.** Backend suite re-run green (114, matching every spec's count); iOS claims verified by reading source + tests (Linux box, suite not run). **(1) HIGH — #6d `bucket_date` derivation (the twelfth trap):** the spec pinned the dedup key to the *evening's* date but prescribed deriving it from `hours[startIndex].localDay` — for a nocturnal Perfect window starting in the **morning tail** (window entirely after midnight, e.g. Perfect 01–03 inside a `22→4` night), that yields the *morning's* date (evening+1), so jitter shifting a window across midnight re-alerts the same night AND the misfiled key suppresses the real next-night alert (same bug family as the #5b morning-tail card-axis fix, 2026-07-13; the spec's own evening-start test fixture couldn't catch it). Rewritten: `bucket_date = localDay(forecastStart, tz) + dayIndex` calendar days (the derivation `TimeDeriver.dayName` already uses); morning-tail + cross-midnight-jitter regression tests added to §3. **(2) #6d — orphan-morning blind spot recorded as accepted:** after local midnight the in-progress night is invisible (`bucketNightWindow` drops it), so a post-midnight good→perfect upgrade never alerts for that night's remainder and bucket 0/"Tonight" means the *next* evening — bounded, alternative is a second engine exception. **(3) #5c — acceptance §3.3 rested on a precondition #5c deletes:** the #5b re-rate sink guards on `lastFetchedCoordinate != nil`, set only by a prior POST, which the Dubai fallback used to guarantee; in the no-location state the first granted fix would be silently discarded — §1 work item + unit test added (nil = always-meaningful). **(4) #6c — empty-snapshot 400 trap:** "same rules as the rating body" made deleting the last Activity (client re-upserts on any mutation) a 400, stranding a stale snapshot that pushes deleted Activities forever; §4 now accepts empty `activities: []` as a valid *dormant* registration (`evaluateAll(hours, [])` → jobs no-op), with the non-empty rule a per-caller concern like the ceiling. **(5) #6c/CONTEXT — hardcoded week-ahead "buckets 2–6"** violated the per-activity `days.length` doctrine (diurnal is usually 8 buckets → tail-day Perfect invisible to every push since the detector stops at 1; nocturnal can be 6 → out-of-bounds) → now `2..days.length−1`, plus an explicit note that bucket 1's absence from the digest is a deliberate ADR-0006 trade-off (tomorrow-Perfect = detector; tomorrow-Good = in-app only). **(6) #6c — pg `DATE` type trap:** `pg` returns `DATE` columns as JS `Date` objects and `dateObject < 'YYYY-MM-DD'` is always false (string→NaN), so a JS-side sent-today comparison = one digest per device *ever*; spec now mandates SQL-side comparison (or TEXT) and a `Date`-object-shaped test fake. **(7) #6c — digest selector self-healing:** strict `localHour === 6` gave one eligible tick/day, so an in-process-cron restart across it (Railway redeploys on every push to `main`) silently dropped a timezone band's digests for the day → selector now `6 ≤ h < 12` catch-up (the `last_digest_date` marker already makes it idempotent; past noon skip); cache wording harmonized (jobs + upsert tz-resolution, never `/rating`). **(8) Doc hygiene:** STATUS §5's surviving "metrics route (needs ADR-0006)" fixed (the one straggler of the de-numbering) + the same in #5b spec ×4 and the `MetricCatalog.swift` TODO comment; CONTEXT.md tense-fixed to the §1 truth rule (push "is built" → designed/lands; Dubai fallback "is deleted" → deleted *by* #5c, built chain still ends in the constant at `DashboardViewModel.swift:16`) and the Digest entry de-hardcoded; #6b's APIConfig path corrected (`ios/TimeIt/TimeIt/Networking/`, lines 4–9) + `TODO(#6)`→`TODO(#6b)`; #6c's "old spec's skeleton" now points at git history. **Verified clean under pressure:** `endIndex` exclusive (`findLongestWindow` `i+1`), `window` input-only, `timeBoundary` exports `localDay`/`localHour` over arbitrary instants returning `'YYYY-MM-DD'`, `errorBody`+502/500 inline in `rating.js` with extraction preconditions exact, `server.js` 400/413 middleware genuinely inherited by future routers, no consumer mutates tagged hours (job cache-share safe), engines `>=18` (bump real), `GeocodingProviding`/`LocationProviding` shapes as claimed, `StaticLocationProvider` nil + both named XCUI tests + `settings.searchButton` + shared-plist `NSAllowsLocalNetworking` all as claimed; zero dead links in the five documents; no surviving GitHub #5/#6 or deleted-#6-spec references beyond intentional supersession notes. **Not verifiable from the tree (build-time checks):** `apns2`'s env-PEM config + whether 410 `Unregistered`/`BadDeviceToken` are distinct signals; `node-cron` behavior. Also logged one cosmetic worldwide limitation (half-hour zones render `ha` labels :30 off in `TimeDeriver` and will in the digest twin) in STATUS §5 + #6c §7. No ADR changes (0006 untouched — findings were spec-level).
- **2026-07-18 (Figma design system v1 built)** — Built the full multi-page design system in the Figma file **Main - Time-it** (`t3ZRvcYPnSRPKElSLAFqmG`) per [`docs/design/figma_foundations_multi-page_implementation.md`](design/figma_foundations_multi-page_implementation.md) (grill same day; palette owner-approved; scope expanded live: redesign latitude on feel/spacing/layouts + all 9 wizard frames): `Primitives`/`Semantic` (Light/Dark) /`Layout` variable collections (scoped + iOS code syntax), Cover/Color/Typography/Spacing foundations pages (band-profile table documented), 9 component sets (redesigned as-built four — Header gains Temp=Cool/Mid/Hot with variable-bound gradient stops — plus Showcase Card, Template Row, Range Picker Row + Range Status, Review Row), 32 screens (16 Light + 16 Dark, safe-area furniture from the iOS 18 kit, incl. the first-launch showcase dashboard and all 9 wizard frames). Legacy `App Colors` collection **kept, not retired** — the owner's wizard-mind-map arrows bind to it (noted on the 🎨 Color page). `Theme colors`, the as-built page, and owner WIP untouched. Spec bannered ✅ IMPLEMENTED.
- **2026-07-19 (backfill — 2026-06-22, Phase 1 implemented + merged; recovered from the retired STATUS banner/§5 checklists)** — Phase 1 (output side) built on `rebuild/phase-1-day-bucketing` and merged into `pre-merge-main`: `fetch.js` → `/api/v1/flexi/point` + `timezone=auto`; `parse.js` `slice(0, FORECAST_HOURS)` (168 ceiling, last day partial, never fabricate hours); new `src/weather/timeBoundary.js` (`localDay` tagging + `zonedWallTimeToUtcIso`); `evaluateAll` output → per-activity `days[]` (global indices) + top-level `timezone` (signature still `evaluateAll(hours)` — activities hardcoded until Phase 2); tests rewritten with the golden snapshot hand-verified against ADR-0004 + a partial-day-0 route fixture; the `days[]`/`timezone`/horizon doc-slice of CONTEXT/CLAUDE migrated in the same commit.
- **2026-07-19 (backfill — 2026-06-24, Phase 2 implemented + merged; recovered from the retired STATUS banner/§5 checklists)** — Phase 2 (request flip + caller-supplied engine + night-stitch) built on `rebuild/phase-2-custom-activities` to ADR-0005 + the ADR-0003/0004 amendments, merged into `pre-merge-main` (`--no-ff`, `ae3e906`): new `src/weather/metricCatalog.js` (live vs coming-soon, validation source of truth); new `src/routes/validateRatingRequest.js` (atomic, structured ADR-0005 §6 rejection set); `rating.js` → `POST { lat, lon, activities[] }`, validate-before-`getWeather`, uniform `{ errors[] }` envelope across 400/502/500; `evaluateAll(hours, activities)` caller-supplied with per-activity bucketing (same-day filter + wrapped night-stitch, orphan-morning drop, global indices hand-verified); `timeBoundary.js` also tags `localHour`; `src/activities/*` deleted (`cli.js` repointed to an inline sample); suite green at 110. Ultra-review run 2026-06-30, its two regressions fixed in `f983179`; merged to `main` 2026-07-03 (`9a9c60b`); Phase-2 doc-slice of CONTEXT/CLAUDE migrated with the code.
- **2026-07-19 (doc-hygiene pass: mockup deleted, spec lifecycle, one-owner-per-fact)** — (1) **Figma Make React mockup deleted from `ios/`** (its own README's deletion condition — "once the SwiftUI app exists" — long met; the Figma file is now the visual reference): removed `src/`, vite/pnpm/postcss configs, `default_shadcn_theme.css`, `ATTRIBUTIONS.md`; new minimal `ios/README.md`; `Guidelines.md` scope-bannered (v1 shipped-UI truth; tab-bar/sign-in/PRO rows are mockup relics of cut features; next iteration lives in Figma); `design-decisions` mockup section marked historical. (2) **Two-agent cross-contamination audit** (docs→code adversarial + code→docs QA): every load-bearing constant/claim verified against `ios/TimeIt` + `src/` (backend 114 re-run green); **no blockers — greenfield for #6b confirmed**; findings were lifecycle-class only. (3) **Lifecycle fixes:** #5a/#5b specs bannered ✅ BUILT (with the `label(for:)`→`MetricCatalog.displayName(for:)` drift note) and **moved to `completed/`** with every inbound link repointed; `design-decisions` "#5b to be written" line fixed; #8 spec STALE-bannered (pre-rebuild language + the 2026-07-15 threshold-shape↔metric-kind scope growth) and #7/#8 dangling issue-3 links fixed; #5c reconciliation list expanded (+`testLateGpsFixTriggersRefetchWhenFirstLoadUsedTheFallback`, +`testGpsFixNearTheFetchedCoordinateDoesNotRefetch`, +`UITEST_RESET` must wipe the new `lastResolvedLocation` key); CLAUDE.md `timeBoundary` bullet now lists `localDay`; GLOSSARY wizard frame count corrected to the as-built 9. (4) **One-owner-per-fact dedup:** STATUS banner slimmed from ~1,600 words of layered "Prior:" history to a pointer line (narratives merged here — incl. the 06-22/06-24 backfills above); STATUS §5's Phase-1/2 resumability checklists retired to the backfill entries; the §5 ▶ NEXT block trimmed of restated build summaries; ROADMAP #5a/#5b lines slimmed to status-pointer one-liners; CLAUDE.md build-order Done line slimmed to pointers; `design-decisions` chip-tier table replaced by a pointer to the canonical (test-pinned) copy in `Guidelines.md`; this file gained its contamination-warning header.
