readme Human Audit:

Line 7:

elludes that the user gets a push notification serendipitously when infact, the user sets the **range** themselves and the conditions of the weather return from the backend to then the client-side where it is asssessed under the **from** **to** time that is set by the user.

Line 11: How it works

fetch and parse is good but everything under **match** and including assumes that we send push notifications serendipitously when a window appears. This idea was discarded.
