# STATUS — project orientation hub

> **Last updated: 2026-08-01 (late evening)** — **#6d BACKEND BUILT** on branch `issue-6d-perfect-window-detector`, pending audit ([spec](issues/current/implement-spec-issue-6d-perfect-window-detector.md), [handoff to auditor](issues/current/handoff-6d-detector-audit.md)): `notification_state` dedup ledger added to `initDb` (PK `(device_id, activity_id, bucket_date)`, `ON DELETE CASCADE`); hourly Perfect-window detector job — Perfect-only, buckets 0–1, `bucket_date` through `timeBoundary.bucketDate` (never window indices, never `hours[startIndex].localDay` — the two audited re-alert traps), insert-first `ON CONFLICT DO NOTHING` with push only on actual insert, ended-window skip on the exclusive `endIndex` bound, ongoing "Now until" copy, end-of-pass pruning — wired beside the digest on the same hourly tick (shared weather cache ⇒ one provider call per location per hour across both jobs). Backend suite 154 → **171**, green in 3 host TZs. Earlier same day — **#6c BACKEND BUILT, owner-audited, merged to `main`** ([spec](issues/current/implement-spec-issue-6c-registration-and-digest.md), [audit handoff](issues/current/handoff-6c-backend-audit.md)): Postgres `devices` + idempotent `initDb`; `PUT`/`DELETE /api/v1/devices/:deviceId` snapshot upsert (empty-`[]` = valid dormant snapshot); shared `validateActivities` + error-envelope extractions (rating output byte-identical); shared 60-min weather cache **now also serving `/rating`** (§6 amendment honoured); `apns2` seam with `StaleTokenError`; hourly digest job (6–11 catch-up band, pg `DATE` type trap handled, nocturnal "tonight", per-activity week-ahead); backend suite 114 → 154. **Not done (both #6c and #6d):** owner's Railway/APNs manual steps (the Railway Postgres add-on now exists and injects `DATABASE_URL`; the APNs credential steps remain); live acceptance checks; iOS §9 opt-in/registration (deferred — needs APNs entitlement + the pbxproj in flight with the WeatherKit track). Earlier same day — **#5c COMPLETE** ([spec](issues/completed/implement-spec-issue-5c-location-onboarding.md)): Dubai fallback deleted; Active-location chain (home → GPS → last-resolved cache → honest no-location empty state); CTA-gated permission prompt (never at launch); `MKLocalSearch` city picker (as-you-type, worldwide) replacing the #5b Settings search; header location line. Figma-first (owner-approved frames on the Screens pages → SwiftUI mirror, TDD), owner-audited pre-push ([findings F1–F6](issues/completed/handoff-5c-audit-findings.md), all fixed) and real-device verified. Also: error state rebuilt with explicit Theme colors (was near-unreadable on device); `accent/interactive #007aff` documented in Guidelines.md. **Where things stand:** backend (through #10 + Phase 1/2 + #6b deploy) and iOS #5a/#5b/#5c are built; the Figma design system v1 was built 2026-07-18 ([spec](design/figma_foundations_multi-page_implementation.md), bannered ✅ IMPLEMENTED); wizard + location-screen design iteration is being brainstormed on the Figma "mess around" page ([handoff](issues/current/handoff-figma-design-iteration.md)); a WeatherKit provider-abstraction track is in flight ([handoff](issues/current/handoff-weatherkit-provider-abstraction.md) — do not touch its files). **▶ Next: audit #6d → owner Railway/APNs steps → iOS §9 push opt-in client** — details in §5. Build order + per-issue status: [ROADMAP](issues/ROADMAP.md). Session-by-session history: [STATUS_LOG](STATUS_LOG.md) — ⚠️ stale-by-design; do not read it without explicit user permission.

This is the **"start here" dashboard** for the codebase. It tells you *where the project is right now* and links down to the detail. It does **not** restate decisions — it points to them, so it can't drift out of sync.

**Read order:** you should already have read [`CLAUDE.md`](../CLAUDE.md) and the glossary in [`CONTEXT.md`](CONTEXT.md) **before** this file. The terms below assume that vocabulary.

---

## 1. Truth rule — which doc to trust

| Doc | Authoritative for |
|---|---|
| [`CLAUDE.md`](../CLAUDE.md), [`CONTEXT.md`](CONTEXT.md) | **Code as it EXISTS now.** The backend through rebuild Phase 2. |
| [`personalization_grill.md`](personalization_grill.md), [`adr/`](adr/) | **Decisions of record.** The Phase 1/2 contract decisions (ADR-0002 → ADR-0005) are now **built**; the push architecture ([ADR-0006](adr/0006-device-keyed-push-evaluation.md)) is **built server-side (#6c merged; #6d pending audit) — the iOS opt-in client is not**; still undecided-in-writing: the `GET /api/v1/metrics` route ADR (unwritten, unnumbered). |

The core contract and the code are now **in sync** — Phase 1/2 merged each doc-slice together with its code. Where the design-docs still lead the code, it is only the *unbuilt remainder* above (expected, not drift). Trust the code-docs for *what is*; trust the design-docs for *what's decided* (built or pending).

## 2. Drift table — terms/contract changing under the rebuild

| Thing | Current (in code) | Locked future | Migrates in | Source |
|---|---|---|---|---|
| **Activity** | **caller-supplied profile sent in the POST body; engine agnostic (`src/activities/*` removed) — migrated** | (now current) | **Phase 2 ✅ (branch)** | [ADR-0002](adr/0002-activity-agnostic-engine.md), grill Q2 |
| **Forecast** | **7-day rolling, ≤168 provider-determined (`flexi` ~161–168) — migrated** | (now current) | **Phase 1 ✅ (branch)** | [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md), grill Q6 |
| **Index** | **0..N-1 (N ≤ 168) — migrated** | (now current) | **Phase 1 ✅ (branch)** | [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md) |
| **Lite / Pro** | **metric-access + quantity gating, client-enforced (no `-lite`/`-pro` activity variants) — migrated** | (now current) | **Phase 2 ✅ (branch)** | grill Q3 |
| **Display metrics** | **user-chosen (request field, echoed through) — migrated** | (now current) | **Phase 2 ✅ (branch)** | [ADR-0002](adr/0002-activity-agnostic-engine.md), grill Q2 |
| **`/rating` contract** | **migrated (in + out):** `POST` body `{ lat, lon, activities[] }`, caller-supplied activities, atomic structured `{ errors[] }`; response `activities[].days[]` + top-level `timezone` (`hour` dropped) | (now current) | **out → Phase 1 ✅, in → Phase 2 ✅ (branch)** | [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md), [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md) (out), [ADR-0005](adr/0005-custom-activity-request-schema.md) (in) |

**All rows are now migrated on the rebuild branches** (Phase 1 = `rebuild/phase-1-day-bucketing`; Phase 2 = `rebuild/phase-2-custom-activities`) — [`CONTEXT.md`](CONTEXT.md)/[`CLAUDE.md`](../CLAUDE.md) describe the built state, so each row's "current" reality matches what those docs say (the drift tripwire holds). They landed on `main` via `pre-merge-main` (merged 2026-07-03, `9a9c60b`). **"Migrates in"** names the phase whose *merge* carries each doc-slice (code + docs together, so `main` never sits in drift); the `/rating` row spanned both phases (response shape in Phase 1, request flip in Phase 2). No Phase-2 drift rows remain open.

## 3. Locked decisions (detail is in the linked source — not repeated here)

- No accounts; guest/local-first; iCloud sync; anon device id for push — [ADR-0001](adr/0001-no-accounts-guest-first.md), grill Q1
- Activity-agnostic engine; curated list → Templates — [ADR-0002](adr/0002-activity-agnostic-engine.md), grill Q2
- Pro = premium metrics + quantity; client-enforced — grill Q3
- Pre-launch data: Air Quality + Marine swappable adapters — grill Q4
- Drop Supabase; one Node + Railway Postgres; append-only events at launch — grill Q5
- Forecast 7-day rolling, flat hours (count provider-determined, **168 = ceiling not count**); day-bucketed evaluation — [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md), grill Q6
- Provider-specifics stop at the adapter boundary; no provider horizon baked into the contract; never fabricate hours — [CONTEXT.md](CONTEXT.md) (Adapter), [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md)
- Day-bucketed `/rating` wire shape: `activities[].days[]`, day-0 card, dense null days, no top-level pointer — [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md)
- Day bucketing = **forecast-location local calendar days** (provider IANA `timezone` + time-boundary module; `dayIndex` 0..6 or 0..7, today/tail partial, `startIndex`/`endIndex` global); response carries top-level `timezone`; client renders in that zone — [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md), [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md)
- Card default = **`days[0]` only — the soonest-actionable roll-forward is CANCELLED** (2026-07-20): a null day 0 renders the card none-state ("No window today"/"No window tonight"); week browsing = detail timeline; still no server field, `bestDayIndex` stays rejected — [ADR-0004 amendment 2026-07-20](adr/0004-day-bucketed-rating-wire-shape.md). *(The old "Daily Digest stays day-0" note is superseded: the Digest is day-0 + week-ahead Perfect highlights — [ADR-0006](adr/0006-device-keyed-push-evaluation.md).)*
- **Request schema (input twin of B2):** POST `{ lat, lon, activities[] }`; per-activity display-superset/threshold-subset; half-open local-hour window (wrap = nocturnal); atomic structured-array 400; **coming-soon/unknown metric = hard 400** (false-Perfect guard); tier/quantity client-enforced, not server-side — [ADR-0005](adr/0005-custom-activity-request-schema.md)
- Wrap-gated **night-stitch** = one controlled cross-midnight exception (opt-in, bounded); nocturnal buckets by *night* so `days.length` is **per-activity** (no cross-activity equality) — [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md) + [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md) amendments
- Notification type follows activity shape, not tier — the grill's "Window Watch" is now the **Perfect-window alert**; full push design pinned in [ADR-0006](adr/0006-device-keyed-push-evaluation.md) + the #6c/#6d specs (2026-07-16)
- **Device-keyed server-side push** — anonymous full-snapshot upsert, opt-in, Postgres at #6c, in-process cron (single replica), `apns2` seam, per-device local-6am Digest, bucket-date dedup — [ADR-0006](adr/0006-device-keyed-push-evaluation.md)
- **Worldwide launch posture; Dubai fallback deleted** — Active-location chain ends in a grayed empty state + onboarding CTAs, never a fabricated location; push registration requires a real location — [CONTEXT.md](CONTEXT.md) (Active location), [#5c spec](issues/completed/implement-spec-issue-5c-location-onboarding.md) (built 2026-08-01)
- iOS shape: 5 surfaces / 8 screens, no bottom bar — grill Q6–Q9, "Page inventory — final (v1)"

## 4. Open blockers & flags (from the grill audit, 2026-06-18)

- **B2 — RESOLVED (2026-06-19).** Day-bucketed `/rating` wire shape pinned → [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md) (read `days.length` per activity; never assume 7). **No longer blocks iOS #5a.** History → [STATUS_LOG.md](STATUS_LOG.md).
- **Provider verification — Meteosource base DONE (2026-06-19)** → [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md) (`flexi` ~164 clean hourly; **168 = ceiling, not count**; point `fetch.js` at `/api/v1/flexi/point`). History → [STATUS_LOG.md](STATUS_LOG.md). **Open:** verify Air Quality + Marine sources return clean hourly across the same horizon — each when its adapter lands, not before.
- **"Stateless backend" is path-specific — now pinned ([ADR-0006](adr/0006-device-keyed-push-evaluation.md), 2026-07-16).** The `/rating` path stays stateless; the push path holds **Device snapshots** (no tier flag — Pro is deferred, so nothing tier-gated runs server-side). The old "push store holds a tier flag / stops Pro cron jobs" wording is dead.
- **Stored-activity metric flip — server side now built; CLIENT reconciliation still undesigned.** The **coming-soon hard-400** is live ([ADR-0005](adr/0005-custom-activity-request-schema.md) + `src/weather/metricCatalog.js`): a threshold/display on a non-live metric is rejected, and because validation is **atomic**, one stale stored activity would **reject the whole `/rating` request** (the entire dashboard fails to render, not just that card). The remaining gap is **client-side**: a reconciliation rule (detect the flip against the served catalog, prompt/strip before POSTing). Sharper now that the structured `{ errors[] }` `path` identifies the offending activity, though a stable error-`code` enum is still wanted (below). — grill Q2 #2 + ADR-0005 atomicity.
- **`GET /api/v1/metrics` route — unpinned (needs an ADR — unwritten and unnumbered; 0006 is now the push ADR).** The catalog **data** now exists server-side (`src/weather/metricCatalog.js`, live vs coming-soon), but the **route** (response envelope, error shape, per-region availability, refresh/TTL) is in no ADR. **No longer blocks the #5b authoring metric-picker** (decided 2026-07-12): #5b ships a **client-side static catalog behind a `MetricCatalogProviding` swap seam** (a hand-mirror of the LIVE set in `metricCatalog.js`), so a `RemoteMetricCatalog` conformer drops in with zero call-site changes when the route lands. Route still **deferred (cleanly parallel — see §5)**; it would read from the existing `metricCatalog.js`. — grill Q2(a)/Q7 + 2026-06-20 audit.
- **#5b/Phase-2 prerequisites surfaced by the 2026-06-20 audit (none block #5a-core):** (a) **ADR-0005 error-`code` enum** — error messages are English prose; the atomic metric-flip recovery needs stable codes for the client to find the offending activity; (b) **seed Template threshold values** — the onboarding seed activities' actual numbers are unpinned (needed once the client authors/POSTs). Both ride with authoring/Phase-2, not #5a-core. *(2026-07-12: the #5b spec's LIVE-only picker makes an in-app-authored activity unable to trigger the metric-flip 400 at all, so the error-`code` enum (a) is a nice-to-have for #5b, not a hard prerequisite; seed thresholds (b) stay provisional and are pinned during the #5b build.)* *(2026-07-13: #5b built — thresholds carried into the Template catalog still marked provisional in `SeedTemplates.swift`; final numbers remain an owner call.)*
- **Grill-only open items (design, not yet folded elsewhere):** (a) **v1-vs-fast-follow launch sequencing** — what ships at first launch vs fast-follow was never decided (grill PENDING #2); (b) **Template-override + client sync schemas** — the custom-activity *request* schema is built ([ADR-0005](adr/0005-custom-activity-request-schema.md)), but the Template-derived override representation and the iCloud/anon-device sync model are still undesigned (grill PENDING #1 remainder, distinct from the client stale-activity reconciliation above). Previously tracked only in [`personalization_grill.md`](personalization_grill.md); moved here so the grill can stay a frozen rationale record. — grill Q4/Q6, cold-start PENDING. *(2026-07-12: #5b **defers cloud sync entirely** — persistence is local-only, `ActivityStore`/UserDefaults — so the sync-schema design stays open but out of the #5b build; the Template-derived override is realised concretely as the `AuthoredActivity` model with a `templateOrigin` field.)*

## 5. Build-readiness

> **▶ NEXT (start here, fresh session): audit the #6d detector** ([handoff](issues/current/handoff-6d-detector-audit.md), branch `issue-6d-perfect-window-detector`) **→ owner Railway/APNs steps → iOS §9 push opt-in/registration client** ([#6c spec](issues/current/implement-spec-issue-6c-registration-and-digest.md) backend built + owner-audited + merged 2026-08-01; [#6d spec](issues/current/implement-spec-issue-6d-perfect-window-detector.md) backend built 2026-08-01; [#6b](issues/completed/implement-spec-issue-6b-railway-deploy.md) and [#5c](issues/completed/implement-spec-issue-5c-location-onboarding.md) are ✅ complete) — all four specs recreated 2026-07-16 from the #6 grill against [ADR-0006](adr/0006-device-keyed-push-evaluation.md); each is self-contained, compact between sub-issues (#6a accounts/auth is CUT per ADR-0001; #6c's header lists the Apple Developer prerequisites). **#5c facts #6c relies on:** `PreferencesStore.homeLocation` + `lastResolvedLocation` (both persisted `SavedLocation`, optional `region` field) are the real-location sources registration reads; the permission prompt is CTA-gated (`LocationProviding.requestAuthorization()` — loads never prompt); the XCUI location args are `UITEST_LOCATION` / `UITEST_LOCATION_DENIED`, and `UITEST_RESET` wipes all three persisted keys. **What a #6-wave session should know:** (a) the iOS app POSTs the **user's persisted `ActivityStore` list** (seeded with the two #5a Templates on first launch) — `SeedTemplates` is a Template *catalog*, not the shipped list; (b) authored windows (incl. nocturnal wrap) go over the wire, so #6c's cron/digest logic must not assume diurnal activities; (c) the metric picker is LIVE-only via `StaticMetricCatalog` — when `GET /api/v1/metrics` (its ADR still unwritten/unnumbered) lands, ship a `RemoteMetricCatalog` conformer of `MetricCatalogProviding` (zero call-site changes); (d) seed/Template **thresholds remain provisional** (owner may edit numbers in `SeedTemplates.swift`); (e) `figure.run` (Running Template icon) is ⚠︎-flagged pending the owner's SF-Symbols-app pass; (f) Pro/StoreKit stays deferred (#5b spec §8) behind the plain `ActivityStore.softCap` constant; (g) **Issue #8 grew a work item** (2026-07-15 fresh-session review): `validateRatingRequest` never checks that a threshold's *shape* matches its metric's *kind* — a `type:"flag"` threshold on a numeric metric passes validation and then never fails an hour in `checkThreshold` (silent false-Perfect); the server metric catalog needs kind awareness (the client mirror already rejects this); (h) **#6c's weather cache scope + TTL amended 2026-07-20 (owner decision, pre-build):** the spec originally scoped `getCachedWeather` to the push jobs only, leaving `/rating` calling the provider fresh on every request — amended so `/rating` shares the same cache (live dashboard traffic, not just the hourly crons, is the largest source of duplicate provider calls); TTL raised 30→**60 min** per an owner-confirmed Meteosource upstream refresh cadence of 10min–1hr (see [`docs/API_documentation/meteosource/README.md`](API_documentation/meteosource/README.md), the new home for vendor API reference docs — one dir per adapter, linked from `CLAUDE.md`). Full detail in the #6c spec §6/Decisions-made/acceptance criteria and the #6d spec's job section — read those before touching `src/services/weatherCache.js` or `src/routes/rating.js`'s default DI wiring. **Current test/tooling facts:** the XCUI suite is **hermetic** (DEBUG-only `MockRatingService` behind `UITEST_MOCK_SUCCESS`/`UITEST_MOCK_FAILURE` launch args — the repo carries no `.env`/Meteosource key); the live path was verified manually 2026-07-12 and the decoder is pinned by a committed **real 166-hour night-spanning response fixture**; the Xcode project is hand-written (Xcode 16 synchronized-folder format, no xcodegen) with the **shared scheme committed on purpose** (`*.xcscheme` deliberately not gitignored — `xcodebuild test` needs it). **Cleanly parallel / not yet built:** `GET /api/v1/metrics` route + the `RemoteMetricCatalog` conformer; client-side stale-activity reconciliation (§4 — unreachable for in-app-authored activities, the picker is LIVE-only); ADR-0005 error-`code` enum; final (non-provisional) Template threshold numbers; Pro/StoreKit wave (post-#7, #5b spec §8). **Advisory (not blockers):** the 2026-06-30 deep-modules review's 3 internal-seam deepenings — module shape, not the wire contract. **Known limitation (not a bug):** DST *inside* a time-of-day window is untested (Asia/Dubai has no DST) — do not assert DST-correctness for windows. **Known limitation (cosmetic, worldwide posture):** half-hour zones (e.g. Asia/Kolkata +05:30) render `ha`-style clock labels :30 off in the iOS `TimeDeriver`, and #6c's digest copy (its server-side twin) will inherit the same — noted in the #6c spec §7.

- **Current backend (through #10):** stable, tested, buildable.
- **iOS #5a (core app):** **BUILT (2026-07-06), LIVE-VERIFIED + AUDITED (2026-07-12) — merged to `main`.** SwiftUI/MVVM at `ios/TimeIt/`, iOS 17+, no third-party packages, per the reconciled [`implement-spec-issue-5a-ios-core.md`](issues/completed/implement-spec-issue-5a-ios-core.md) + [`design-decisions-issue-5.md`](issues/current/design-decisions-issue-5.md) + [`ios/guidelines/Guidelines.md`](../ios/guidelines/Guidelines.md). Ships exactly the #5a slice: POSTs the two client-side seed Templates (Cycling + Fishing-lite, live metrics only), decodes variable-length per-activity `days[]` + top-level `timezone` (nothing hardcodes 7; all clock/day labels render in the response zone via a single `TimeDeriver`), card = day 0 only with global-index window positioning on the day's real hour span (roll-forward cancelled 2026-07-20, ADR-0004 amendment), detail = timeline over all of `days[]`; GPS with silent Dubai fallback; 502 vs 500 surfaced distinctly. No accounts, no tab bar, no PRO. Night-stitch decoding is contract-ready client-side (per-activity `days.length`).
- **iOS #5b (authoring & personalization):** **BUILT (2026-07-13, TDD, this tree).** Adds the authoring layer on the #5a app per [`implement-spec-issue-5b-ios-personalization.md`](issues/completed/implement-spec-issue-5b-ios-personalization.md): `AuthoredActivity`/`ActivityStore` (UserDefaults, seeded, soft cap 10), ghost add-card → `AddActivityView` (Template copy or from-scratch) → `ActivityEditorView` (metric picker over the `MetricCatalogProviding` seam, threshold editor, optional window incl. nocturnal wrap), card gear = edit, delete → empty state that **skips the POST**, `PreferencesStore` home location (geocoder seam) winning over GPS, nocturnal "Tonight"/"… night" labels, `ActivityIconView` icon seam. iOS suite now **138 tests** (124 unit + 14 XCUI, incl. `UITEST_RESET` isolation). No StoreKit, no cloud sync, no PRO badge (all deferred by the locked scoping calls). Audited twice: same-session `/code-review` (4 correctness bugs fixed pre-commit) + a fresh-session independent review on 2026-07-15 ([findings](review/issue-5b-authoring-code-review.md), all 7 fixed on `issue-5b-authoring`).
- **Timeline detail (screen #2):** unblocked for base weather — Meteosource `flexi` 7-day hourly verified. (Marine/AQ fields on the timeline wait for those adapters.)

### Rebuild Phases 1 + 2 — complete

Both merged to `main` 2026-07-03 (`9a9c60b`; ultra-review regressions fixed in `f983179`). What each phase built → [STATUS_LOG](STATUS_LOG.md) (2026-06-22 / 2026-06-24 entries); the resulting architecture → [`CLAUDE.md`](../CLAUDE.md); the contracts → [ADR-0003](adr/0003-seven-day-horizon-flat-hours-day-buckets.md) / [ADR-0004](adr/0004-day-bucketed-rating-wire-shape.md) / [ADR-0005](adr/0005-custom-activity-request-schema.md).

---

## History

Full chronological edit history → [STATUS_LOG.md](STATUS_LOG.md) — ⚠️ contamination-warned: stale by design, read only with explicit user permission. This file holds only current state + forward pointers.
